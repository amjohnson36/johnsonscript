# Program to demonstrate array use. Creates a new array and passes it to function that writes into it and then reads it.
# make arrays : Displays the program
# make arraysx: Executes the program

def array(var arr) 
start
    var i = 0.
    while (i < 15) start
        if ((i % 2) == 0) start
            setarray(arr, i, " is even").
        stop
        else start
            setarray(arr, i, " is odd").
        stop
        i = i + 1.
    stop 
    
    while (not (i == 0)) start
        i = i - 1.
        var b = getarray(arr, i).
        print(i + b).
    stop
stop

var myArray = newarray(15).
array(myArray).
