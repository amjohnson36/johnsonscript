# Program to demonstrate the file input in johnsonscript. There are 4 built in functions for handling file input.

def sum(var fname)
start
    var s = 0.
    var file = openfile(fname).
    var i = readinteger(file).
    while (not (atfileend(file))) start
        s = s + i.
        i = readinteger(file).
    stop
    print("Sum is " + s).
    closefile(file).
stop

sum("ints").
